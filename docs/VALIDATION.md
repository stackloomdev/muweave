# 无状态 ChatGPT MCP 接入

2026-09-24：新增独立远程 MCP 和 MCP Apps 画布，不引入账户库、工程存储、素材上传或服务器渲染。独立网页仍可纯静态运行。

- 类型、26 项单元/协议测试、网页与 MCP 生产构建、格式检查通过；插件 manifest 与 skill 校验通过。
- MCP 测试由官方 Client 通过真实 loopback HTTP 执行：工具发现、只读公共组件、24 个并发客户端使用相同工程/场景/请求 ID 仍各自保留内容；按 ID 查找、错误工程 ID/版本、非法整批修改、媒体引用、额外身份参数、大快照与超大 HTTP 请求均被拒绝。相同输入重试结果一致，响应不缓存，也不签发会话 ID；补测 Vercel helpers 已读取请求体的兼容路径，不混用预解析正文。
- 9 项 Chromium 回归全部通过（34.6 秒）。新增两项验证两个 iframe 画布互不影响，其他 iframe 不能冒充宿主替换结果；工程 ZIP 经宿主下载桥生成，真实网页文件选择导入后产生新 ID，并成功导出 1920 × 1080 PNG。无下载能力时显示 JSON 交接；两次导入同 ID 的 JSON 仍创建两个新工程，拒绝带媒体引用的 JSON。
- 原有字体、编辑、素材、字幕和真实 1080p ffmpeg.wasm 视频导出回归保持通过。网页无 API、无外网的原测试也保持通过。

这些结果证明当前实现的请求/组件隔离，不代表带身份认证的多租户系统，也不代表真实 ChatGPT 沙箱已经验收。首版只能处理调用方提供的完整无素材快照；不能判断该快照原本属于谁，不能查询其他对话的最新版本。ChatGPT 账户内接入、宿主下载权限和目录上架仍需目标账户验收。构建仍有原主 JS 超过 500 kB 提示，聊天组件内联 HTML 约 907 KiB（未压缩）。

---

# 当前验证：完整浏览器版本

2026-09-23。应用运行层已移除 Fastify、Node 文件存储、原生 FFmpeg 和无头 Chromium。Vite 仅提供静态资源；尚未执行公网部署。

- 类型检查、19 项 core / IndexedDB 测试、生产构建通过；包含事务回滚、持久去重、跨连接并发、撤销/重做、素材原子写入、冻结快照、取消后终态保护和非法导出参数。
- 7 项真实 Chromium 端到端测试全部通过（约 30 秒）。覆盖真实文件选择导入音频/图片、测量与静音建议、图层拖拽和缩放、场景删除/撤销、Google 字体、4:3 PNG、SRT 下载和 ZIP 往返。
- 实际浏览器 MP4：1920 × 1080 / 30 FPS / 4.2 秒 / 126 帧，H.264 + AAC，浏览器完整解码和元数据检查通过；测试工具另以原生 FFprobe 和全片解码独立复核。2 秒编码批次和 2.1 秒场景边界附近均有连续音频能量。
- 字体与视频专项禁止 `/api/` 和站外 HTTP 请求仍通过；素材来自 IndexedDB Blob，静态字体、Worker、WASM 从同源加载。
- 导出期间编辑不改变快照；跨标签取消、立即重试、同 ID 错误参数重试不解锁、刷新后成品可预览/下载、活动页面不被误判关闭、关闭后任务标记中断均通过。
- 独立只读审查发现并修复请求锁归属和 MP4 误带封面参数问题；额外修复终态预检跨 await 的清锁竞态，最终再通过类型、构建和完整 WASM 专项。
- 旧磁盘目录三个工程已通过只读迁移脚本生成独立工程包，源工程、历史和成片没有修改。

内置浏览器生产构建验收：旧业务进程已停止，原 4175 端口只运行 Vite 静态预览。原生 WebMCP 实际发现 16 个工具并成功执行能力查询、文件选择交接、浏览器 WAV 解码/测量、素材事务和撤销。三个旧工程均通过浏览器文件选择导入；原 FLAC 配音保留哈希、时长和微秒时间轴，`project.validate` 无问题。

同一中文三页配音示例，在无业务后端的生产页面中由 WebMCP 发起 MP4 导出：398 帧 / 13.2667 秒 / 1920 × 1080 / H.264 + AAC；浏览器生成及完整检查耗时 22.5023 秒，core 加载 0.2528 秒，826,013 bytes（约 0.79 MiB）。7 个批次，最大暂存 PNG 11,878,440 bytes。成品保存到 IndexedDB，通过 Blob URL 在页面预览；播放器实测 `videoWidth=1920`、`videoHeight=1080`、`readyState=4`、时长 13.266667 秒，无页面错误。未据此宣称长片听感或全部浏览器兼容已验证。

仍未验证：公网 Vercel 部署、长项目总内存、移动端/Safari/Firefox、完整十页配音项目听感。构建仍有主 JS 超过 500 kB 提示；WASM 首次原始下载约 31 MiB。公开分发的 ffmpeg.wasm 许可与对应源码打包仍需发布前完成。

## 线上字体加载优化

2026-09-23：线上原版的中文字体响应为完整 TTF，17,772,300 bytes，缓存头为 `max-age=0, must-revalidate`。已改用 Google Fonts 官方 WOFF2 可变字体与 `unicode-range` 分片；110 份文件共 4,707,048 bytes，所有下载地址、Unicode 范围与 SHA-256 随源码记录。运行时同源加载，内容哈希文件名配合 Vercel 一年 immutable 缓存。构建不依赖 Google 在线服务。

同机 Chromium、全新浏览器上下文和空缓存，生产构建模拟 10 Mbps 下载 / 40 ms 延迟：默认三页示例的字体下载由 18,260,884 bytes（2 个请求）降至 858,928 bytes（17 个请求），减少约 95.3%；画布显示从 15,127 ms 降至 1,110 ms。两次均无页面错误。这是本地受控网络的单次对比，尚不能代表各地区的公网冷启动速度。

类型检查、19 项单元测试、生产构建、格式检查和全部 7 项 Chromium 回归通过。字体专项禁止站外和 API 请求，检查首屏加载预算和 WOFF2 按需请求；随后新增“龘”，阻断其未加载分片，确认 PNG 导出保持等待，释放响应后才完成。真实 1080p WASM 视频导出与字幕验证保持通过。

以下保留此前架构的历史测试证据；其中本地 runtime 和 native 导出已由上面的浏览器版本替代。

---

# 历史验证记录

日期：2026-09-23。版本：0.1.0，early preview。环境：macOS，Node.js 22.12.0，pnpm 10.30.3，Playwright 1.58.2，FFmpeg / FFprobe。

## 已通过

| 检查                | 结果与范围                                                             |
| ------------------- | ---------------------------------------------------------------------- |
| `pnpm check`        | TypeScript、13 项核心/服务测试、生产构建通过                           |
| `pnpm test:e2e`     | 6 项端到端测试通过，Chromium + 真实本地服务 + FFmpeg，总耗时约 12.5 秒 |
| `pnpm format:check` | 全部通过                                                               |
| `pnpm run doctor`   | Node、FFmpeg、FFprobe、Chromium 均可用                                 |
| 新仓库运行          | 从独立仓库启动，重新启动后已保存工程可读，Codex 内置浏览器可打开       |

核心测试覆盖：部分属性修改保留其他样式、批量失败不写入、无效排序和陈旧版本拒绝、配音裁剪与字幕同步、固定场景时长时的字幕偏移、100 场景累计时间不插入隐式停顿、独立封面、SRT 校验、请求持久去重、撤销/重做及重启恢复、并发版本冲突、Host/Origin/会话与无效命令拒绝。

浏览器端到端覆盖：场景改名、刷新保留、撤销、独立封面、新增场景；等比图片拖拽后逻辑框尺寸与位置正确；实测音频/图片导入、首尾静音建议、字幕随裁剪移动、导出时继续编辑、工程包导出并重新导入、预览播放/暂停、运行中取消编码后再次导出成功。

场景删除回归：在 1054 × 720 窗口、右侧切换到文案面板时，左侧“删除场景”仍完整可见。新增场景删除后自动选中前一页并校正播放位置，其他场景内容保持一致；撤销/重做、缩略图聚焦后的 Delete、最后一页保护及刷新持久化均通过。Codex 内置浏览器已刷新检查新入口。

## 编辑器视觉与交互打磨

统一浅色工作台的颜色、字号、按钮与输入框尺寸；合并顶部重复横栏，放大场景缩略图，固定场景操作区，重新整理画布、属性面板、播放控制、时间轴和素材/导出弹窗的层级。窄窗口保留缩放与圆形工具，属性切换和图层选择后回到面板顶部。

- Codex 内置浏览器实际查看当前窗口的新版布局。
- 独立临时示例实测 900 × 660、1054 × 720、1440 × 900：页面无横向溢出，画布完整，工具栏、播放与删除按钮均在可视范围内。检查属性、图层、文案、素材、导出及 4:3 封面状态，无页面运行错误。
- 修复选中框控制点随画布缩小而过大的问题。Konva Transformer 使用屏幕坐标，控制点、边线和留白不再重复除以画布缩放比例。
- 新增真实鼠标回归：从已滚动的图层列表选择文字后，文字输入框完整可见；拖动侧边控制点时，即使鼠标同时有纵向位移，也只修改宽度，保持高度、Y 坐标和字号。

公开截图使用独立原创示例，不包含个人素材或验收临时工程。

## Google Fonts 字体替换

内置字体统一为 Google Fonts 官方仓库的 Roboto 与 Noto Sans SC，固定上游提交并记录字体和许可证的 SHA-256。移除 Fontsource 依赖与原 Manrope 分发文件，随字体保留完整 OFL 1.1 授权原文。

- 旧工程、封面与字体修改命令中的旧名称会映射到新字体；首次读取不改写工程文件或增加版本，撤销和重做保留原有历史。
- 浏览器实测字体选项、切换与保存；实际加载的只有本地 Roboto 和 Noto Sans SC，拦截外部请求后仍正常工作。
- 含中英文混排的 Roboto 图层成功导出 1920 × 1080 PNG；导出页面也实际加载了同样两款字体。原有真实 MP4 完整解码、工程包导入和导出取消回归全部通过。
- Codex 内置浏览器刷新后核对界面字体、可见画布和页面宽度；中文使用 Noto Sans SC，英文与数字使用 Roboto。

## 原生 WebMCP 实机

通过 Codex Browser 插件控制内置浏览器打开本地工作台，实际发现页面注册的 15 个工具。没有安装 `modelContext` polyfill，也没有以直接调用私有页面对象代替工具执行。

验证过程：

1. 原生工具创建独立验证工程，读取场景及标题节点。
2. `node.update` 修改标题，回读文字变化且字号仍为 106；撤销后 revision 从 1 变为 2。
3. 通过工具领取上传地址，宿主上传一张已有真实封面，`assets.import` 提交素材，再通过 `scene.add` 将图片放入画布。
4. 刷新后工程 revision 4、素材和新增场景仍存在，并在可见画布确认图片。
5. 重启运行服务、重新发现工具，在原创示例上调用 `project.validate` 返回空问题列表。
6. 原生 `export.start` → `jobs.get` 完成一张 1920 × 1080 PNG，状态 `succeeded`，revision 与提交一致。

导入的个人封面仅存于本地运行数据，未放入源码或公开样例。

## 输出检查

自动化媒体 fixture 是一段实际生成的 4 秒 WAV：开头约 300 ms、句内约 350 ms、结尾约 500 ms 静音。导入后测量时长为 4,000,000 µs。首尾分析保留起止安全余量，不删除句内停顿。该 fixture 验证时间与媒体管线，不代表完整真人口播试听。

一次成功导出的任务报告：

```json
{
  "revision": 5,
  "frames": 162,
  "fps": 30,
  "durationSeconds": 5.4,
  "width": 1920,
  "height": 1080,
  "codec": "h264",
  "fullDecode": "passed",
  "renderSeconds": 2.92
}
```

4:3 PNG 实际文件尺寸为 1440 × 1080。工程包重新导入后获得新 ID，两个素材和裁剪后的字幕保留。取消测试等待编码产生非零进度后再取消，随后另一个 PNG 导出成功。

## 发现并修复

- Zod 部分更新中的默认值曾可能覆盖未修改属性，已改为移除默认值后构造 patch schema，并加回归测试。
- 等比图片显示偏移可能混入拖拽坐标，改为固定逻辑框绘制，并通过真实鼠标拖拽测试。
- 同时变换多个节点改为合并提交，避免各节点争抢同一版本。
- 固定场景时长时，裁剪同一音频也会同步字幕；编码取消时处理 stdin 关闭事件。
- 导出读取快照的素材接口，避免编辑中的撤销影响正在导出的素材解析。
- 初次端到端测试在最后的同名按钮定位处超时，已限定工程选择对话框；修正后 3 项全部通过。

## 未完成的验证

完整十页真人配音项目和全片听感、长项目性能、跨平台安装、远程 CI、断电恢复、进程被强制终止时的导出恢复、导入自定义字体、复杂编组与多选变换、逐像素预览/导出比对尚未全部验证。相关实现存在不等于上述场景均已验收。

构建通过但有单个 JS chunk 超过 500 kB 的体积提示，目前未作为功能阻断。页面截图见 `docs/screenshots/studio.png`；测试临时工程和视频不纳入源码分发。

## 浏览器 ffmpeg.wasm 导出试用

2026-09-23：`@ffmpeg/ffmpeg 0.12.15` + 单线程 `@ffmpeg/core 0.12.10`。生产构建把 worker、core JS 与 WASM 放在同源静态资源中，按需加载。当前仍需要本地 runtime 提供工程、素材导入处理、快照和成片落盘；不是纯静态部署验收。

- `pnpm check`：类型、15 项核心/服务测试、生产构建通过。新增冻结版本、runner 归属、取消后终态保护与升级前导出请求去重回归。
- 全部 7 项 Playwright 浏览器测试通过，用时约 30 秒。最后对导出锁归属和预览布局作了小修正，再次通过类型、构建、WASM 专项测试及格式检查。
- WASM 专项使用实际图片、中文、字幕和 48 kHz 连续音频，生成 4.2 秒 / 126 帧 / 1920 × 1080 / H.264 + AAC MP4。另用本机 FFprobe 与完整解码复核帧数/时长，并检查 2 秒编码批次、2.1 秒场景边界两侧的 PCM 能量，没有引入额外静音。
- 导出期间修改画面，成片仍绑定已冻结版本。阻断非本地网络后仍成功加载字体与 WASM。编码中取消、图片响应挂起时取消、立即重新导出成功，未出现页面异常。
- 审查后增加 job 图片缓存清理、AbortSignal 网络取消、按请求归属释放导出锁，避免重复导出的资源和锁残留。约 24 MiB 阈值只覆盖 PNG 批次，不能当作浏览器总内存。
- Core 0.12.10 的 ffprobe 在完整输出 JSON 后出现返回 -1 的退出行为；当前先严格完整解码，再校验探测 JSON 的帧数、帧率、尺寸、音视频编码与有限时长，不能仅凭 JSON 文件存在视为成功。

Codex 内置浏览器通过原生 WebMCP 实际完成 `app.get_capabilities`、创建独立中文配音示例、版本化配音/字幕命令、`project.validate`、`export.start(engine=wasm)` 和 `jobs.get`。样例采用系统 Tingting 合成的三句测试语音与原创三页画布，按音频实测时长设置场景，没有添加页间停顿。样例保存在本地工程目录，未加入源码。

| 实机指标             | WASM 导出结果                                                   |
| -------------------- | --------------------------------------------------------------- |
| 分辨率/帧率          | 1920 × 1080 / 30 FPS                                            |
| 成片时长/帧数        | 13.2667 秒 / 398 帧                                             |
| 浏览器生成与校验耗时 | 24.808 秒，含 0.2163 秒 core 加载，不含最终保存请求             |
| 文件大小             | 826,013 bytes，约 0.79 MiB                                      |
| 编码批次             | 7 批，最多 60 帧/批                                             |
| 最大暂存 PNG 字节    | 11,878,440 bytes，约 11.3 MiB；总内存峰值未测                   |
| 媒体检查             | 浏览器完整解码和元数据校验通过；本机独立解码与 FFprobe 复核通过 |

同一冻结版本另行执行本机模式，398 帧、同分辨率与时长完整解码通过；原有报告的绘帧/编码/校验耗时为 7.319 秒（不含 Chromium 启动，与 WASM 报告时间范围略有区别，不能作为严格吞吐基准）。这台机器上短片 WASM 导出可用，但速度明显较慢。

仍未验证：完整十页知识视频的全片听感、十分钟以上/大量高分辨率图片的内存、移动端、离开页面后自动恢复、静态 Vercel 部署。首次 WASM 原始资源约 31 MiB，公网冷加载耗时未测。本次为本机试用，公开构建分发的依赖许可证与对应源码打包仍在待办。
